# Backend Service Family Instructions

- Apply this instruction set to repositories with `projectFamily = backend-service`.
- Check API contract, validation, security, config, DB compatibility, and rollout impact together.
- Treat Java 11 and Spring Boot 2.3.x as the default baseline unless a documented exception exists.
- Combine this with `api.instructions.md` when the repo has an `api` runtime role.
