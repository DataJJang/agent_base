# Receiver Integration Family Instructions

- Apply this instruction set to repositories with `projectFamily = receiver-integration`.
- Preserve ingress, parser or decoder, publish targets, payload samples, and diagnostics together.
- Treat Java 11 and Spring Boot 2.7.x as the default baseline unless a documented exception exists.
- Combine this with `receiver.instructions.md` when the repo has a `receiver` runtime role.
