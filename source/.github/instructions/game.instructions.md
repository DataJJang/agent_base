# Game Family Instructions

- Apply this instruction set to repositories with `projectFamily = game`.
- Preserve engine version, boot scene, validation entrypoints, and target platform assumptions together.
- Treat Unity LTS and C# as the default baseline unless a documented exception exists.
- Keep client, tooling, and optional backend split explicit in docs.
