# Mobile App Family Instructions

- Apply this instruction set to repositories with `projectFamily = mobile-app`.
- Keep platform config, signing, package naming, env handling, and backend auth dependencies aligned.
- Treat Flutter as the default baseline unless a documented exception exists.
- Require device or simulator smoke validation when platform behavior changes.
