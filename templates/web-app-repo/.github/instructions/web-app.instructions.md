# Web App Family Instructions

- Apply this instruction set to repositories with `projectFamily = web-app`.
- Treat `TypeScript + React + Vite` as the default baseline unless a documented exception exists.
- Preserve route, env, API binding, and screen-level smoke assumptions together.
- Combine this with `frontend.instructions.md` when the repo has a `frontend` runtime role.
