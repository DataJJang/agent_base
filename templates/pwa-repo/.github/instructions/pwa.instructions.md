# PWA Family Instructions

- Apply this instruction set to repositories with `projectFamily = pwa`.
- Preserve installability, offline behavior, manifest, and cache strategy together.
- Do not change service worker or offline assumptions without updating docs and smoke validation.
- Combine this with `frontend.instructions.md` when the repo has a `frontend` runtime role.
