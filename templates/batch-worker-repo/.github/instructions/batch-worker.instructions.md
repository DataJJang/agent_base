# Batch Worker Family Instructions

- Apply this instruction set to repositories with `projectFamily = batch-worker`.
- Preserve scheduler, job, mapper, SQL ownership, retry, history, and runbook assumptions together.
- Treat Java 11 and Spring Boot 2.7.x as the default baseline unless a documented exception exists.
- Combine this with `batch.instructions.md` when the repo has a `batch` runtime role.
