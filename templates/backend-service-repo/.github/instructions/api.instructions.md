# API Path Instructions

- Apply this instruction set to the `api` runtime role.
- Preserve controller, service, model, repository, and query-repository boundaries.
- Check config, validation, security, DB compatibility, and rollout impact together.
- Read `docs/ai/services/api.md` before structural service changes.
- If the repository family is `backend-service`, combine this with the backend-service family instruction.
