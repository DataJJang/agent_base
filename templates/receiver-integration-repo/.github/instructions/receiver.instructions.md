# Receiver Path Instructions

- Apply this instruction set to the `receiver` runtime role.
- Preserve ingress, parser or decoder, service, publish, and config boundaries.
- Check payload validation, protocol routing, publish flow, and runtime diagnostics together.
- Read `docs/ai/services/receiver.md` before structural receiver changes.
- If the repository family is `receiver-integration`, combine this with the receiver-integration family instruction.
